// 1. Write a JavaScript program to count and display the items of a dropdown list(select tag options).
function dropdrown_list() {
    let radius = document.getElementById('radius').value;
    let value = document.getElementById('volume').value;
    
    alert (radius * value);
}